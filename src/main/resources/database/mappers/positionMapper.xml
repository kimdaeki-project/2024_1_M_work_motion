<?xml version="1.0" encoding="UTF-8"?>
		<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		    
<mapper namespace="com.workmotion.app.position.PositionDAO">
		
		<select id="selectPosition" parameterType="MemberDTO" resultType="PositionDTO">
		SELECT * FROM position P WHERE p.company_id = #{company_id} 
		</select>
	























</mapper>